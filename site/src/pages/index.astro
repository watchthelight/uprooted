---
import Layout from "../layouts/Layout.astro";
---

<Layout title="uprooted">
  <div class="page">
    <div class="header">
      <h1>uprooted <span class="version">0.4.0</span></h1>
      <p class="sub">a client mod framework for root communications.</p>
    </div>

    <div class="features-grid">
      <div class="feature"><span class="fname">custom themes</span><span class="tag">working</span></div>
      <div class="feature"><span class="fname">sentry blocker</span><span class="tag">working</span></div>
      <div class="feature"><span class="fname">plugins</span><span class="tag">working</span></div>
      <div class="feature"><span class="fname">bridge api</span><span class="tag">working</span></div>
      <div class="feature"><span class="fname">settings injection</span><span class="tag">working</span></div>
      <div class="feature"><span class="fname">link embeds</span><span class="tag tag-beta">beta</span></div>
      <div class="feature"><span class="fname">message logger</span><span class="tag tag-beta">beta</span></div>
      <div class="feature"><span class="fname">auto-updater</span><span class="tag">working</span></div>
      <div class="feature"><span class="fname">silent typing</span><span class="tag tag-beta">beta</span></div>
      <div class="feature"><span class="fname">clearurls</span><span class="tag">working</span></div>
    </div>

    <p class="blurb">
      <strong>working beta.</strong> themes and sentry blocker ship by default. all features are configurable from root's native settings panel.
      root's <a href="https://root.gg/terms">terms</a> prohibit reverse engineering — we believe cosmetic mods can coexist with that policy.
    </p>

    <div class="cta">
      <a href="https://github.com/watchthelight/uprooted/releases/latest" class="download-btn">download</a>
      <nav>
        <a href="https://github.com/watchthelight/uprooted">source</a>
        <span class="dot">&middot;</span>
        <a href="https://rootapp.gg/AC0ILwUxgQqJ2MOSMXdGjw">server</a>
        <span class="dot">&middot;</span>
        <a href="https://github.com/watchthelight/uprooted/blob/main/LICENSE" class="dim">license</a>
      </nav>
    </div>

    <div class="tos-box">
      <div class="tos-label">terms of use</div>
      <ol class="tos">
        <li><strong>do not distribute</strong> uprooted or its artifacts outside of the official repository.</li>
        <li><strong>do not discuss</strong> uprooted in Root's public channels, servers, or support.</li>
        <li>violations will result in your UUID being <strong>permanently blacklisted</strong>.</li>
      </ol>
      <p class="tos-note">to share uprooted, send a link to the <a href="https://github.com/watchthelight/uprooted">repository</a>.</p>
    </div>

    <p class="footer dim">not affiliated with root communications.</p>
  </div>

  <div class="mobile-block">
    <p>sorry, uprooted is only available on windows and linux.</p>
  </div>
</Layout>

<style>
  .mobile-block {
    display: none;
  }

  .page {
    position: relative;
    z-index: 1;
    max-width: 680px;
    margin: 0 auto;
    padding: 0 32px;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 20px;
  }

  .header {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  h1 {
    font-size: 22px;
    font-weight: 700;
    letter-spacing: 0.04em;
    color: var(--text);
  }

  .version {
    font-size: 12px;
    font-weight: 400;
    color: var(--dim);
    letter-spacing: 0.02em;
  }

  .sub {
    color: var(--dim);
    font-size: 13px;
  }

  /* 2-column feature grid */
  .features-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6px 32px;
    padding: 16px 0;
    border-top: 1px solid var(--faint);
    border-bottom: 1px solid var(--faint);
  }

  .feature {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    font-size: 13px;
    color: var(--text);
    min-width: 0;
  }

  .fname {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .tag {
    flex-shrink: 0;
    font-size: 10px;
    color: var(--green);
    border: 1px solid rgba(106, 154, 110, 0.3);
    padding: 1px 6px;
    letter-spacing: 0.04em;
    white-space: nowrap;
  }

  .tag-beta {
    color: #c89040;
    border-color: rgba(200, 144, 64, 0.3);
  }

  /* inline blurb */
  .blurb {
    font-size: 13px;
    line-height: 1.65;
    color: var(--dim);
  }

  .blurb strong {
    color: var(--text);
  }

  /* CTA row: download + nav inline */
  .cta {
    display: flex;
    align-items: center;
    gap: 20px;
    flex-wrap: wrap;
  }

  .download-btn {
    display: inline-block;
    background: var(--green);
    color: #08080c;
    font-family: inherit;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.04em;
    padding: 8px 24px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: background 0.15s;
    flex-shrink: 0;
  }

  .download-btn:hover {
    background: #7fb882;
    text-decoration: none;
  }

  nav {
    font-size: 13px;
  }

  .dot {
    color: var(--dim);
    margin: 0 6px;
  }

  .dim {
    color: var(--dim);
  }

  /* ToS box */
  .tos-box {
    border: 1px solid rgba(200, 80, 80, 0.25);
    background: rgba(200, 80, 80, 0.04);
    padding: 14px 18px;
  }

  .tos-label {
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #c85050;
    margin-bottom: 8px;
  }

  .tos {
    list-style: none;
    font-size: 12px;
    color: var(--text);
    line-height: 1.7;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .tos li strong {
    color: #c85050;
  }

  .tos-note {
    font-size: 11px;
    color: var(--dim);
    margin-top: 8px;
  }

  .footer {
    font-size: 11px;
    padding-bottom: 24px;
  }

  @media (max-width: 640px) {
    .page {
      display: none;
    }

    .mobile-block {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      padding: 32px;
      text-align: center;
      font-size: 14px;
      color: var(--dim);
      position: relative;
      z-index: 1;
    }
  }

  /* dyslexia mode — tighten to keep in viewport */
  :global(html.dyslexia) .page {
    gap: 16px;
  }

  :global(html.dyslexia) .features-grid {
    gap: 4px 32px;
  }

  :global(html.dyslexia) .blurb,
  :global(html.dyslexia) .sub {
    font-size: 12.5px;
    line-height: 1.6;
  }

  :global(html.dyslexia) .tos {
    font-size: 11.5px;
  }
</style>
