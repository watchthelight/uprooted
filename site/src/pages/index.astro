---
import Layout from "../layouts/Layout.astro";
---

<Layout title="uprooted">
  <div class="page">
    <div class="main">
      <div class="left">
        <div class="header">
          <h1>uprooted <span class="version">0.4.0</span></h1>
          <p class="sub">a client mod framework for root communications.</p>
        </div>

        <p class="blurb">
          <strong>working beta.</strong> themes and sentry blocker ship by default.
          all features configurable from root's native settings panel.
          root's <a href="https://root.gg/terms">terms</a> prohibit reverse engineering —
          we believe cosmetic mods can coexist with that policy.
        </p>

        <div class="cta">
          <a href="https://github.com/watchthelight/uprooted/releases/latest" class="download-btn">download</a>
          <nav>
            <a href="https://github.com/watchthelight/uprooted">source</a>
            <span class="dot">&middot;</span>
            <a href="https://rootapp.gg/AC0ILwUxgQqJ2MOSMXdGjw">server</a>
            <span class="dot">&middot;</span>
            <a href="https://github.com/watchthelight/uprooted/blob/main/LICENSE" class="dim">license</a>
          </nav>
        </div>
      </div>

      <div class="right">
        <div class="features-grid">
          <div class="feature"><span class="fname">custom themes</span><span class="tag">working</span></div>
          <div class="feature"><span class="fname">sentry blocker</span><span class="tag">working</span></div>
          <div class="feature"><span class="fname">plugins</span><span class="tag">working</span></div>
          <div class="feature"><span class="fname">bridge api</span><span class="tag">working</span></div>
          <div class="feature"><span class="fname">settings injection</span><span class="tag">working</span></div>
          <div class="feature"><span class="fname">link embeds</span><span class="tag tag-beta">beta</span></div>
          <div class="feature"><span class="fname">message logger</span><span class="tag tag-beta">beta</span></div>
          <div class="feature"><span class="fname">auto-updater</span><span class="tag">working</span></div>
          <div class="feature"><span class="fname">silent typing</span><span class="tag tag-beta">beta</span></div>
          <div class="feature"><span class="fname">clearurls</span><span class="tag">working</span></div>
        </div>
      </div>
    </div>

    <div class="tos-box">
      <span class="tos-label">terms of use</span>
      <div class="tos-items">
        <span><strong>do not distribute</strong> uprooted or its artifacts outside of the official repository.</span>
        <span><strong>do not discuss</strong> uprooted in Root's public channels, servers, or support.</span>
        <span>violations will result in your UUID being <strong>permanently blacklisted</strong>.</span>
      </div>
      <span class="tos-note">to share uprooted, send a link to the <a href="https://github.com/watchthelight/uprooted">repository</a>.</span>
    </div>

    <p class="footer dim">not affiliated with root communications.</p>
  </div>

  <div class="mobile-block">
    <p>sorry, uprooted is only available on windows and linux.</p>
  </div>
</Layout>

<style>
  .mobile-block {
    display: none;
  }

  .page {
    position: relative;
    z-index: 1;
    max-width: 1020px;
    margin: 0 auto;
    padding: 0 48px;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 28px;
  }

  /* === main: left (identity) | right (features) === */
  .main {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 72px;
    align-items: center;
  }

  .left {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .header {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  h1 {
    font-size: 22px;
    font-weight: 700;
    letter-spacing: 0.04em;
    color: var(--text);
  }

  .version {
    font-size: 12px;
    font-weight: 400;
    color: var(--dim);
    letter-spacing: 0.02em;
  }

  .sub {
    color: var(--dim);
    font-size: 13px;
  }

  .blurb {
    font-size: 13px;
    line-height: 1.7;
    color: var(--dim);
  }

  .blurb strong {
    color: var(--text);
  }

  .cta {
    display: flex;
    align-items: center;
    gap: 18px;
    flex-wrap: wrap;
  }

  .download-btn {
    display: inline-block;
    background: var(--green);
    color: #08080c;
    font-family: inherit;
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 0.04em;
    padding: 8px 22px;
    text-decoration: none;
    cursor: pointer;
    transition: background 0.15s;
    flex-shrink: 0;
  }

  .download-btn:hover {
    background: #7fb882;
    text-decoration: none;
  }

  nav {
    font-size: 13px;
  }

  .dot {
    color: var(--dim);
    margin: 0 6px;
  }

  .dim {
    color: var(--dim);
  }

  /* === features grid === */
  .right {
    border-left: 1px solid var(--faint);
    padding-left: 48px;
  }

  .features-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px 24px;
  }

  .feature {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    font-size: 13px;
    color: var(--text);
    min-width: 0;
  }

  .fname {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .tag {
    flex-shrink: 0;
    font-size: 10px;
    color: var(--green);
    border: 1px solid rgba(106, 154, 110, 0.3);
    padding: 1px 6px;
    letter-spacing: 0.04em;
    white-space: nowrap;
  }

  .tag-beta {
    color: #c89040;
    border-color: rgba(200, 144, 64, 0.3);
  }

  /* === tos: single compact row === */
  .tos-box {
    border: 1px solid rgba(200, 80, 80, 0.2);
    background: rgba(200, 80, 80, 0.03);
    padding: 10px 16px;
    display: flex;
    align-items: baseline;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 11.5px;
  }

  .tos-label {
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #c85050;
    font-size: 10px;
    flex-shrink: 0;
  }

  .tos-items {
    display: flex;
    flex-wrap: wrap;
    gap: 0 20px;
    color: var(--text);
    flex: 1;
  }

  .tos-items strong {
    color: #c85050;
  }

  .tos-note {
    color: var(--dim);
    flex-shrink: 0;
  }

  .footer {
    font-size: 11px;
  }

  /* === mobile === */
  @media (max-width: 640px) {
    .page {
      display: none;
    }

    .mobile-block {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      padding: 32px;
      text-align: center;
      font-size: 14px;
      color: var(--dim);
      position: relative;
      z-index: 1;
    }
  }

  /* === dyslexia mode === */
  :global(html.dyslexia) .page {
    gap: 22px;
  }

  :global(html.dyslexia) .main {
    gap: 56px;
  }

  :global(html.dyslexia) .blurb,
  :global(html.dyslexia) .sub {
    font-size: 12.5px;
    line-height: 1.65;
  }

  :global(html.dyslexia) .features-grid {
    gap: 6px 24px;
  }

  :global(html.dyslexia) .tos-box {
    font-size: 11px;
  }
</style>
